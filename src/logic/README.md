# logic

Pure clinical rules with no React or database code: derived case status, labour
progress (NICE intrapartum care), flags and reminders, gestation and BMI. Every
rule has unit tests next to it (`*.test.ts`).
